we need to do a full codebase revamp.  no code before i say. only plan mode.
check the folder structure of the worker and suggest files names and organization based on that.
we need everything to be modular, neatly organized and most importantly readble.
you may have to re-evaluate the existing structuring of this app afte these changes. the flow needs to be template based now. no one by one delivery.

Meta’s Instagram Private Replies API allows a maximum of 1 private-reply message per comment (i.e., you can only send a single DM “private reply” tied to that comment).

Additional relevant constraint: the private reply must be sent within 7 days of the comment’s creation time.
We are not handling anything related to Instagram Live. Strip off anything that handles that.

Meta's Instagram Private-Reply API allows only one private-reply message per comment. The code can send image, text, and links sequentially—all targeted at comment_id for non-STORY_REPLY automations (lines 79–80, 110–111). This means automations with replyMessage + dmLinks, or with image + text, or any multi-part response will fail on the second message.

Consolidate multiple content types into a single initial private reply.

After all the checks are done. Create a generic template (keep the templates seprately. in a react like component structure.) and only fill the parts that are available for that automation. For ex - A user could set an automation on a Post-A with an image, text, bunch of links. Now instead of sending each thing one by one, keep a generic template ready somewhere, import that, see which parts of that template are available, construct the template and send it in one go.

  replyMessage       String // Fixed DM message
  replyImage         String? // Uploaded image URL for DM
  dmLinks            DmLink[] // Multiple links for DM

these are the parameters that go inside the template.

heres the official meta example req -

{
  "recipient":{
    "id":"{{ig_scoped_id}}"
  },
  "message":{
    "attachment":{
      "type":"template",
      "payload":{
        "template_type":"generic",
        "elements":[
           {
            "title":"Welcome!",
            "image_url":"https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRN2z0ERwXQUqH29urPuzWueLXKhJAY6SMyAA&s",
            "subtitle":"We have the right hat for everyone.",
            "default_action": {
              "type": "web_url",
              "url": "https://www.originalcoastclothing.com",
            },
            "buttons":[
              {
                "type":"web_url",
                "url":"https://www.originalcoastclothing.com",
                "title":"View Website"
              },{
                "type":"postback",
                "title":"Start Chatting",
                "payload":"DEVELOPER_DEFINED_PAYLOAD"
              }
            ]
          }
        ]
      }
    }
  }
}

Follow the same fashion for the Ask To Follow Gate - A title and the button to profile. maybe we already have that template ready but reverify just in case.

  askToFollowMessage String? // Message sent when commenter doesn't follow yet
  askToFollowLink    String? // Profile link appended to the Ask to Follow message

these are the parameters that go inside the Follow Gate template.

{
    "recipient": {
        "id": {{ig_scoped_id}}
    },
    "message": {
        "attachment": {
            "type": "template",
            "payload": {
                "template_type": "button",
                "text": "You need to follow me first!",
                "buttons": [
                    {
                        "type": "",
                        "url": "",
                        "title": "Visit profile"
                    }
                ]
            }
        }
    }
}


For the opening message -

  openingMessage        String? // Message sent first (Ice-breaker)
  openingButtonText     String? // Text for the interactive button

these are the parameters that go inside the Opening message template.

{
    "recipient": {
        "id": {{ig_scoped_id}}
    },
    "message": {
        "attachment": {
            "type": "template",
            "payload": {
                "template_type": "button",
                "text": openingButtonText,
                "buttons": [
                    // this button is used to start the conversation. see the existing implementation for how it works.
                ]
            }
        }
    }
}



Now coming to the automations related to the stories. Maybe you already know what to do here. Most of the functionality is same. just make sure that in case of stories the public replies are never sent. Maybe we dont even have to send the opening message in that case, because end users already open the 24h window by interacting first.


we dont have to make any changes on the main next app. only the worker app.